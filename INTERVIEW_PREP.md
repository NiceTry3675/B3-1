# Mini Redis 인터뷰 대비 학습 문서

## 1. 한 문장 설명

이 프로젝트는 직접 구현한 해시맵, 이중 연결 리스트, 최소 힙을 조합해 Redis의 String 저장, LRU 메모리 제거, TTL 만료 처리를 CLI로 재현한 Mini Redis입니다.

## 2. 해시맵

`hash_map.py`의 `HashMap`은 체이닝 방식입니다. 버킷 배열의 각 칸에는 이중 연결 리스트가 들어가며, 같은 버킷으로 충돌한 key-value 쌍은 리스트에 연결됩니다.

해시 함수는 문자열의 UTF-8 바이트를 순회하면서 `result = result * 31 + byte` 형태로 누적합니다. 최종 값에 버킷 수를 나눈 나머지를 사용해 인덱스를 구합니다.

로드 팩터가 `0.75`를 초과하면 버킷 배열을 2배로 늘리고 모든 엔트리를 새 capacity 기준으로 다시 배치합니다. capacity가 바뀌면 `hash % capacity` 결과도 바뀌기 때문에 rehash가 필요합니다.

면접 답변 포인트:

- 평균적으로 `put`, `get`, `remove`는 O(1)입니다.
- 최악의 경우 모든 키가 한 버킷에 몰리면 O(N)입니다.
- 충돌 자체는 문제가 아니고, 충돌을 어떻게 관리하느냐가 중요합니다.

## 3. 이중 연결 리스트와 LRU

`linked_list.py`의 노드는 `prev`, `next`, `data`를 가집니다. Mini Redis는 LRU 리스트의 head를 “가장 최근 사용”, tail을 “가장 오래 사용되지 않음”으로 둡니다.

`GET`이 성공하거나 `SET`이 저장에 성공하면 해당 key의 LRU 노드를 head로 이동합니다. Redis 엔트리가 자기 LRU 노드 포인터를 들고 있으므로 리스트를 검색하지 않아도 됩니다.

면접 답변 포인트:

- 해시맵만 있으면 key 조회는 빠르지만 사용 순서를 알 수 없습니다.
- 리스트만 있으면 순서는 알 수 있지만 key 조회가 느립니다.
- 해시맵과 이중 연결 리스트를 함께 쓰면 조회, 이동, 삭제를 평균 O(1)에 처리할 수 있습니다.

## 4. 메모리 제한과 eviction 흐름

`used_memory`는 요구사항 공식대로 `len(utf8(key)) + len(utf8(value))`의 합입니다. 노드, 포인터, 버킷 오버헤드는 계산하지 않습니다.

`SET` 흐름:

1. 새 엔트리 하나가 `maxmemory`보다 크면 저장하지 않고 OOM 에러를 반환합니다.
2. 새 key면 저장하고, 기존 key면 값 크기 차이를 반영합니다.
3. 기존 key를 덮어쓰면 TTL을 삭제합니다.
4. `used_memory > maxmemory`이면 LRU tail부터 삭제합니다.
5. 삭제된 키 수는 `evicted_keys`에 누적됩니다.

## 5. TTL과 최소 힙

TTL은 `(expire_at, key, version)` 형태의 레코드를 `min_heap.py`의 최소 힙에 넣습니다. 가장 빨리 만료될 항목이 heap root에 있으므로 다음 만료 후보를 빠르게 확인할 수 있습니다.

같은 key에 `EXPIRE`가 여러 번 호출될 수 있으므로 lazy deletion 전략을 사용합니다. 새 TTL을 설정할 때 version을 증가시키고, 힙에서 꺼낸 레코드의 version이 현재 expiration map의 version과 다르면 오래된 레코드로 보고 무시합니다.

면접 답변 포인트:

- 힙의 `push`/`pop`은 O(log N), `peek`은 O(1)입니다.
- 모든 key를 매번 순회하며 만료 여부를 찾으면 O(N)입니다.
- 힙은 “가장 이른 만료 시간”을 계속 추적하는 데 적합합니다.

## 6. 명령어 엣지 케이스

- 만료된 key는 명령 실행 전에 삭제되어 없는 key처럼 처리됩니다.
- 만료된 key에 `GET`을 하면 `(nil)`이고 LRU 갱신은 하지 않습니다.
- `TTL`은 key가 없으면 `(integer) -2`, TTL이 없으면 `(integer) -1`입니다.
- `EXPIRE key 0` 또는 음수는 즉시 삭제로 처리합니다.
- `DEL`은 store, TTL metadata, LRU 리스트를 함께 정리합니다.

## 7. 보너스 구현 설명

- `dynamic_array.py`: capacity 2배 확장, `append/get/set/remove` 구현
- `binary_tree.py`: 전위, 중위, 후위, 레벨 순회 구현
- `bst.py`: 삽입, 탐색, 삭제, 중위 순회 정렬 결과 구현
- `pubsub.py`: 채널별 구독자 리스트와 구독자별 메시지 큐 구현
- `STACK_QUEUE_DEQUE.md`: 스택, 큐, 덱 개념과 Redis 확장 연결 설명

## 8. 자주 나올 수 있는 질문

Q. 왜 LRU에 단일 연결 리스트가 아니라 이중 연결 리스트를 쓰나요?

A. 임의 노드를 삭제하거나 앞으로 이동할 때 이전 노드를 알아야 합니다. 단일 연결 리스트는 이전 노드를 찾기 위해 탐색이 필요하지만, 이중 연결 리스트는 `prev` 포인터가 있어 O(1)에 연결을 바꿀 수 있습니다.

Q. TTL 힙에서 기존 TTL을 직접 삭제하지 않는 이유는 무엇인가요?

A. 일반 힙에서 중간 원소를 key로 찾아 삭제하려면 별도 인덱스 맵이 필요합니다. 이 구현은 version을 둔 lazy deletion으로 오래된 TTL 레코드를 꺼낼 때 무시합니다. 구현이 단순하고 `EXPIRE` 갱신이 빠릅니다.

Q. OOM 처리에서 단일 엔트리가 maxmemory보다 크면 왜 바로 거절하나요?

A. 다른 키를 모두 제거해도 그 엔트리 하나만으로 제한을 초과하기 때문입니다. 저장 후 eviction을 해도 조건을 만족할 수 없어 저장하지 않는 것이 맞습니다.

Q. `SET`이 기존 key를 덮어쓰면 TTL을 왜 삭제하나요?

A. 요구사항에서 기존 TTL을 초기화하도록 정했기 때문입니다. Redis의 기본 `SET`도 별도 옵션이 없으면 기존 TTL을 제거하는 동작과 유사합니다.

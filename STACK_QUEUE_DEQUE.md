# 스택, 큐, 덱 학습 정리

## 스택

스택은 마지막에 넣은 데이터가 먼저 나오는 LIFO 구조입니다. 대표 연산은 `push`, `pop`, `peek`입니다.

활용 예시는 함수 호출 스택, 괄호 검사, DFS, 실행 취소입니다. Mini Redis를 확장한다면 최근 명령 히스토리를 되돌리는 기능에 사용할 수 있습니다.

## 큐

큐는 먼저 넣은 데이터가 먼저 나오는 FIFO 구조입니다. 대표 연산은 `enqueue`, `dequeue`, `peek`입니다.

활용 예시는 작업 대기열, BFS, 메시지 처리입니다. 이 과제의 Pub/Sub 보너스에서는 구독자별 메시지 버퍼를 큐처럼 사용할 수 있습니다. `pubsub.py`는 직접 구현한 이중 연결 리스트를 메시지 큐로 재활용합니다.

## 덱

덱은 양쪽 끝에서 삽입과 삭제가 모두 가능한 구조입니다. 대표 연산은 `push_front`, `push_back`, `pop_front`, `pop_back`입니다.

이 과제의 이중 연결 리스트는 덱으로도 볼 수 있습니다. LRU에서는 앞쪽을 최신 사용, 뒤쪽을 오래된 사용으로 정해 `move_to_front`와 `remove_back`을 O(1)에 수행합니다.

## Redis LRU와의 연결

LRU는 “가장 오래 사용되지 않은 키”를 빠르게 찾아 제거해야 합니다. 단순 배열로 구현하면 중간 이동과 삭제가 O(N)이 되기 쉽지만, 이중 연결 리스트는 노드 포인터만 바꾸면 되므로 O(1)에 이동과 삭제가 가능합니다.

해시맵은 key로 Redis 엔트리를 O(1)에 찾고, Redis 엔트리는 LRU 리스트의 노드 포인터를 보관합니다. 그래서 `GET`/`SET` 성공 시 해당 노드를 바로 앞으로 옮길 수 있습니다.

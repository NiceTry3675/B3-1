# 자료구조 쉽게 이해하기

이 문서는 Mini Redis 과제에 등장하는 자료구조를 쉽게 이해하기 위한 설명입니다. 직접 구현한 코드의 세부 동작보다는, 각 자료구조가 어떤 문제를 해결하기 위해 존재하는지와 왜 이 과제에 어울리는지를 중심으로 설명합니다.

## 1. 자료구조가 필요한 이유

자료구조는 데이터를 아무렇게나 담는 그릇이 아니라, 목적에 맞게 데이터를 정리하는 방법입니다.

예를 들어 이런 질문들이 있습니다.

```text
어떤 이름으로 저장한 값을 빠르게 찾고 싶다.
가장 오래 사용하지 않은 데이터를 알고 싶다.
가장 빨리 만료될 데이터를 알고 싶다.
먼저 들어온 메시지를 먼저 처리하고 싶다.
정렬된 순서로 값을 보고 싶다.
```

각 질문에 잘 맞는 자료구조가 다릅니다.

| 하고 싶은 일 | 어울리는 자료구조 |
| --- | --- |
| 이름으로 빠르게 찾기 | 해시맵 |
| 앞뒤로 빠르게 넣고 빼기 | 연결 리스트, 덱 |
| 가장 작은 값 빠르게 찾기 | 최소 힙 |
| 먼저 들어온 것부터 처리 | 큐 |
| 마지막에 넣은 것부터 처리 | 스택 |
| 계층 구조 표현 | 트리 |
| 정렬된 순서 유지 | 이진 탐색 트리 |

Mini Redis 과제는 이런 자료구조들이 실제 기능 안에서 어떻게 쓰이는지 보여주는 예제입니다.

## 2. 동적 배열

배열은 데이터를 번호가 붙은 칸에 저장하는 구조입니다.

```text
index:   0     1     2     3
value: [ A ] [ B ] [ C ] [ D ]
```

배열의 장점은 특정 위치를 빠르게 찾을 수 있다는 점입니다.

```text
2번 칸 주세요 -> 바로 C를 찾음
```

하지만 일반 배열은 처음 정한 크기를 넘어서기 어렵습니다. 그래서 동적 배열은 공간이 부족해지면 더 큰 배열을 만들고 기존 데이터를 옮깁니다.

```text
작은 배열
[ A ][ B ][ C ][ D ]

공간이 부족하면

큰 배열
[ A ][ B ][ C ][ D ][   ][   ][   ][   ]
```

쉽게 말해 동적 배열은 “필요하면 커지는 배열”입니다.

Mini Redis에서는 동적 배열이 다른 자료구조의 바닥 재료처럼 쓰입니다. 해시맵의 버킷 목록이나 힙의 내부 저장 공간을 만들 때 활용됩니다.

## 3. 해시맵

해시맵은 key로 value를 빠르게 찾기 위한 자료구조입니다.

일상 예시로는 사전이 비슷합니다.

```text
apple -> 사과
book  -> 책
cat   -> 고양이
```

Mini Redis에서도 같은 생각을 사용합니다.

```text
user:1 -> Alice
user:2 -> Bob
token  -> abc123
```

해시맵이 빠른 이유는 key를 그대로 처음부터 끝까지 찾는 것이 아니라, key를 숫자로 바꿔서 저장 위치를 계산하기 때문입니다.

```text
"user:1"
   |
   v
해시 함수
   |
   v
저장할 위치 번호
```

### 해시 충돌

서로 다른 key가 같은 위치 번호를 받을 수도 있습니다. 이것을 해시 충돌이라고 합니다.

```text
bucket 0: token
bucket 1: user:1 -> cart:7
bucket 2: session
```

위 예시에서는 `user:1`과 `cart:7`이 같은 칸에 들어갔습니다. 이런 경우 같은 칸 안에서 여러 데이터를 이어서 보관할 수 있습니다. 이 방식을 체이닝이라고 합니다.

핵심은 이것입니다.

```text
해시맵은 key로 데이터를 빠르게 찾기 위한 구조다.
충돌이 생길 수 있으므로 충돌 처리 방법이 필요하다.
```

Mini Redis에서는 `SET key value`, `GET key` 같은 명령을 빠르게 처리하기 위해 해시맵 개념을 사용합니다.

## 4. 연결 리스트

연결 리스트는 데이터들이 줄줄이 연결된 구조입니다. 각 데이터는 다음 데이터가 어디 있는지 알고 있습니다.

```text
[ A ] -> [ B ] -> [ C ] -> [ D ]
```

배열은 칸 번호로 바로 접근하기 좋지만, 중간에 데이터를 넣거나 빼면 뒤의 데이터들을 옮겨야 할 수 있습니다.

```text
배열에서 B 삭제:
[ A ][ B ][ C ][ D ]
      삭제
[ A ][ C ][ D ]      <- C, D를 앞으로 당겨야 함
```

연결 리스트는 앞뒤 연결만 바꾸면 됩니다.

```text
A -> B -> C

B 삭제:
A -> C
```

## 5. 이중 연결 리스트

이중 연결 리스트는 각 노드가 다음 데이터뿐 아니라 이전 데이터도 알고 있는 연결 리스트입니다.

```text
[ A ] <-> [ B ] <-> [ C ]
```

단방향 연결 리스트:

```text
A -> B -> C
```

이중 연결 리스트:

```text
A <-> B <-> C
```

이중 연결 리스트의 장점은 어떤 노드를 알고 있을 때, 그 노드를 빠르게 삭제하거나 앞쪽으로 옮기기 쉽다는 점입니다.

```text
A <-> B <-> C

B를 삭제하려면:
A와 C를 서로 연결

A <-> C
```

Mini Redis에서는 최근 사용 순서를 관리할 때 이중 연결 리스트가 잘 맞습니다. 앞쪽에는 최근 사용한 데이터를 두고, 뒤쪽에는 오래 사용하지 않은 데이터를 둘 수 있기 때문입니다.

## 6. LRU 개념

LRU는 Least Recently Used의 약자입니다. 뜻은 “가장 최근에 사용되지 않은 것”입니다.

캐시나 메모리 제한이 있는 저장소에서는 공간이 부족할 때 무엇을 지울지 정해야 합니다. LRU는 그 기준을 이렇게 정합니다.

```text
가장 오래 안 쓴 데이터를 먼저 지우자.
```

예를 들어 사용 순서가 아래와 같다고 해보겠습니다.

```text
가장 최근 사용                         가장 오래 안 씀
[ user:3 ] -> [ user:2 ] -> [ user:1 ]
```

이때 `user:1`을 다시 조회하면 `user:1`은 방금 사용된 데이터가 됩니다.

```text
조회 전:
[ user:3 ] -> [ user:2 ] -> [ user:1 ]

GET user:1

조회 후:
[ user:1 ] -> [ user:3 ] -> [ user:2 ]
```

이제 메모리가 부족해지면 맨 뒤의 `user:2`가 삭제 후보가 됩니다.

```text
[ user:1 ] -> [ user:3 ] -> [ user:2 ]
                                      ^
                                      가장 오래 안 쓴 데이터
```

Mini Redis에서는 이 LRU 순서를 이중 연결 리스트로 관리합니다.

## 7. 큐

큐는 먼저 들어온 데이터가 먼저 나가는 구조입니다. FIFO라고도 부릅니다. FIFO는 First In, First Out의 약자입니다.

```text
들어오는 방향
   |
   v
[ A ] -> [ B ] -> [ C ]
                 |
                 v
             나가는 방향
```

하지만 보통 큐를 설명할 때는 이렇게 생각하면 쉽습니다.

```text
enqueue A
enqueue B
enqueue C

dequeue -> A
dequeue -> B
dequeue -> C
```

일상 예시는 줄 서기입니다. 먼저 줄 선 사람이 먼저 나갑니다.

```text
앞 [ 사람1 ][ 사람2 ][ 사람3 ] 뒤
```

Mini Redis의 Pub/Sub 메시지 버퍼는 큐처럼 이해할 수 있습니다. 메시지가 순서대로 쌓이고, 사용자는 먼저 도착한 메시지부터 확인합니다.

## 8. 스택

스택은 마지막에 들어온 데이터가 먼저 나가는 구조입니다. LIFO라고도 부릅니다. LIFO는 Last In, First Out의 약자입니다.

```text
push A
push B
push C

pop -> C
pop -> B
pop -> A
```

일상 예시는 접시 쌓기입니다.

```text
맨 위 [ C ] <- 먼저 꺼냄
     [ B ]
맨 아래 [ A ]
```

이 과제의 핵심 Mini Redis 기능에서 스택이 직접 크게 쓰이지는 않지만, 자료구조 학습 범위에서 함께 다루면 큐와 차이를 이해하기 좋습니다.

## 9. 덱

덱은 양쪽 끝에서 넣고 뺄 수 있는 구조입니다. double-ended queue의 줄임말입니다.

```text
앞                                  뒤
push_front 가능               push_back 가능
pop_front 가능                 pop_back 가능

[ A ] <-> [ B ] <-> [ C ]
```

큐는 한쪽으로 들어가고 반대쪽으로 나가는 느낌이 강하지만, 덱은 양쪽 끝을 모두 사용할 수 있습니다.

```text
앞에 넣기
[ X ] [ A ] [ B ]

뒤에 넣기
[ X ] [ A ] [ B ] [ Y ]

앞에서 빼기
[ A ] [ B ] [ Y ]

뒤에서 빼기
[ A ] [ B ]
```

LRU는 덱과 잘 어울립니다. 앞쪽에는 최근 사용한 데이터를 넣고, 뒤쪽에서는 오래된 데이터를 제거하면 되기 때문입니다.

## 10. 힙

힙은 가장 큰 값이나 가장 작은 값을 빠르게 찾기 위한 자료구조입니다.

이 과제에서는 가장 작은 값을 빠르게 찾는 최소 힙을 사용합니다.

최소 힙에서는 부모가 자식보다 작거나 같습니다.

```text
        3
       / \
      7   5
     / \
    9   8
```

가장 작은 값은 항상 맨 위에 있습니다.

```text
root = 3
```

Mini Redis에서는 “가장 빨리 만료될 데이터”를 찾는 데 최소 힙 개념이 잘 맞습니다.

예를 들어 만료 시간이 아래와 같다고 해보겠습니다.

```text
user:1 -> 10초 뒤 만료
user:2 -> 3초 뒤 만료
user:3 -> 7초 뒤 만료
```

가장 먼저 확인해야 할 것은 `user:2`입니다.

```text
        user:2, 3초
       /          \
 user:1, 10초   user:3, 7초
```

힙을 쓰면 “다음에 만료될 데이터가 무엇인지” 빠르게 알 수 있습니다.

## 11. 트리

트리는 계층 구조를 표현하는 자료구조입니다.

```text
        A
       / \
      B   C
     / \
    D   E
```

트리는 부모와 자식 관계를 가집니다.

```text
A는 B와 C의 부모
B는 D와 E의 부모
D와 E는 B의 자식
```

트리는 파일 시스템, 조직도, HTML 구조, 검색 구조 등 계층이 있는 데이터를 표현할 때 자주 사용됩니다.

## 12. 이진 트리

이진 트리는 각 노드가 최대 두 개의 자식을 갖는 트리입니다.

```text
        A
       / \
      B   C
     /     \
    D       E
```

자식을 보통 왼쪽 자식, 오른쪽 자식이라고 부릅니다.

```text
왼쪽 자식  <= 부모 =>  오른쪽 자식
```

이 과제에서는 이진 트리를 통해 순회 방법을 학습합니다.

```text
전위 순회: 현재 노드 -> 왼쪽 -> 오른쪽
중위 순회: 왼쪽 -> 현재 노드 -> 오른쪽
후위 순회: 왼쪽 -> 오른쪽 -> 현재 노드
레벨 순회: 위에서 아래로, 왼쪽에서 오른쪽으로
```

## 13. 이진 탐색 트리

이진 탐색 트리, 즉 BST는 값을 찾기 쉽게 규칙을 추가한 이진 트리입니다.

규칙은 간단합니다.

```text
왼쪽에는 더 작은 값
오른쪽에는 더 큰 값
```

예를 들어 아래 트리는 BST입니다.

```text
        4
       / \
      2   6
     / \ / \
    1  3 5  7
```

`5`를 찾는다고 해보겠습니다.

```text
1. 4와 비교: 5는 4보다 큼 -> 오른쪽으로 이동
2. 6과 비교: 5는 6보다 작음 -> 왼쪽으로 이동
3. 5 발견
```

BST는 중위 순회를 하면 정렬된 순서로 값을 볼 수 있습니다.

```text
1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 7
```

## 14. 시간복잡도 쉽게 이해하기

자료구조를 배울 때 자주 나오는 O(1), O(log N), O(N)은 “데이터가 많아질 때 시간이 얼마나 늘어나는가”를 대략 표현하는 방법입니다.

예를 들어 데이터가 10개일 때는 어떤 방식이든 빨라 보일 수 있습니다. 하지만 데이터가 10만 개가 되면 차이가 크게 납니다.

| 표기 | 쉬운 의미 | 예시 |
| --- | --- | --- |
| O(1) | 데이터 수와 거의 상관없이 한 번에 처리 | 배열 인덱스로 접근, 해시맵 평균 조회 |
| O(log N) | 데이터가 많아져도 아주 천천히 증가 | 힙 삽입/삭제, 균형 잡힌 트리 탐색 |
| O(N) | 데이터 수만큼 살펴볼 수 있음 | 전체 목록 순회 |
| O(N log N) | 정렬에서 자주 등장 | 효율적인 비교 정렬 |
| O(N²) | 데이터가 늘면 급격히 느려짐 | 모든 쌍을 비교 |

숫자로 감을 잡아보면 이렇습니다.

```text
N = 100,000

O(1)      -> 대략 1번
O(log N)  -> 대략 17번 정도
O(N)      -> 100,000번
O(N²)     -> 10,000,000,000번
```

정확한 실행 시간은 컴퓨터 성능, 언어, 구현 방식에 따라 다릅니다. 하지만 자료구조를 고를 때는 “데이터가 커졌을 때도 버틸 수 있는가”를 이 표기법으로 생각합니다.

## 15. 배열과 연결 리스트의 차이

배열과 연결 리스트는 둘 다 여러 값을 순서대로 담을 수 있지만, 잘하는 일이 다릅니다.

배열은 칸 번호를 알면 바로 접근할 수 있습니다.

```text
index:  0    1    2    3
value: [A]  [B]  [C]  [D]

index 2를 주세요 -> 바로 C
```

그래서 배열은 “몇 번째 값인지 알고 있을 때” 강합니다. 하지만 중간 삽입과 삭제는 약할 수 있습니다.

```text
[A][B][C][D]

B를 삭제하면:
[A][C][D]

C와 D를 앞으로 당겨야 함
```

연결 리스트는 각 값이 다음 값의 위치를 알고 있습니다.

```text
A -> B -> C -> D
```

중간 노드를 알고 있다면 연결만 바꿔 삭제할 수 있습니다.

```text
A -> B -> C

B 삭제:
A -> C
```

대신 연결 리스트는 “3번째 값을 바로 주세요” 같은 작업에는 약합니다. 처음 노드부터 하나씩 따라가야 하기 때문입니다.

| 상황 | 배열 | 연결 리스트 |
| --- | --- | --- |
| 인덱스로 바로 접근 | 빠름 | 느림 |
| 맨 뒤에 추가 | 보통 빠름 | tail이 있으면 빠름 |
| 중간 삽입/삭제 | 밀거나 당겨야 할 수 있음 | 노드를 알고 있으면 빠름 |
| 메모리 구조 | 연속된 칸 | 흩어진 노드를 포인터로 연결 |

핵심은 “배열이 항상 좋다” 또는 “연결 리스트가 항상 좋다”가 아니라, 어떤 작업을 많이 하느냐에 따라 선택이 달라진다는 점입니다.

## 16. 해시를 더 쉽게 이해하기

해시는 어떤 값을 숫자로 바꾸는 과정입니다.

```text
"apple" -> 530
"book"  -> 912
"cat"   -> 184
```

이 숫자를 배열 크기로 나눈 나머지를 쓰면 저장 위치를 정할 수 있습니다.

```text
hash("apple") = 530
bucket count = 10
530 % 10 = 0

apple은 0번 칸에 저장
```

해시맵이 빠른 이유는 값을 찾을 때 처음부터 끝까지 뒤지지 않기 때문입니다.

```text
일반 목록:
apple인가? 아니네
book인가? 아니네
cat인가? 찾았다

해시맵:
cat을 해시한다
위치 번호를 계산한다
그 칸으로 바로 간다
```

좋은 해시 함수는 key들을 여러 버킷에 고르게 흩뿌립니다. 나쁜 해시 함수는 많은 key를 같은 버킷에 몰아넣습니다.

```text
좋은 분포:
bucket 0: A
bucket 1: B, C
bucket 2: D
bucket 3: E

나쁜 분포:
bucket 0: A, B, C, D, E
bucket 1:
bucket 2:
bucket 3:
```

해시맵의 평균 조회가 O(1)이라고 말하는 것은 “해시가 어느 정도 고르게 퍼지고, 버킷이 너무 꽉 차지 않는다”는 전제가 있을 때입니다.

## 17. 충돌과 로드 팩터

해시 충돌은 서로 다른 key가 같은 버킷 번호를 받는 상황입니다.

```text
hash("apple") % 5 = 2
hash("grape") % 5 = 2
```

충돌은 잘못된 일이 아니라 자연스러운 일입니다. 저장할 key는 거의 무한히 많을 수 있지만, 버킷 수는 제한되어 있기 때문입니다.

충돌을 처리하는 대표적인 방식은 두 가지입니다.

| 방식 | 설명 |
| --- | --- |
| 체이닝 | 같은 버킷 안에 여러 값을 리스트로 이어서 저장 |
| 오픈 어드레싱 | 충돌하면 다른 빈 칸을 찾아 저장 |

체이닝은 이렇게 이해하면 됩니다.

```text
bucket 0:
bucket 1: apple -> grape -> melon
bucket 2: book
```

로드 팩터는 해시맵이 얼마나 찼는지 나타냅니다.

```text
load factor = 저장된 데이터 수 / 버킷 수
```

예를 들어 버킷이 8개인데 데이터가 6개면 로드 팩터는 0.75입니다.

```text
6 / 8 = 0.75
```

로드 팩터가 너무 커지면 충돌 가능성이 커지고, 한 버킷 안의 리스트가 길어질 수 있습니다. 그래서 많은 해시맵은 일정 기준을 넘으면 버킷 수를 늘립니다.

```text
버킷 8개로 부족해짐
-> 버킷 16개로 확장
-> 기존 key들을 새 버킷 기준으로 다시 배치
```

이 다시 배치하는 과정을 rehash라고 합니다.

## 18. 캐시와 제거 정책

캐시는 자주 쓰는 데이터를 빠르게 꺼내기 위해 가까운 곳에 잠시 보관하는 공간입니다.

예를 들어 웹 서비스에서 사용자 정보를 매번 데이터베이스에서 읽으면 느릴 수 있습니다. 그래서 한 번 읽은 결과를 캐시에 넣어두고 다음 요청에서 빠르게 꺼냅니다.

문제는 캐시 공간이 무한하지 않다는 것입니다. 공간이 부족하면 무엇을 버릴지 정해야 합니다. 이 규칙을 eviction policy, 즉 제거 정책이라고 부릅니다.

대표적인 정책은 다음과 같습니다.

| 정책 | 기준 | 쉬운 설명 |
| --- | --- | --- |
| FIFO | 먼저 들어온 것 | 오래 전에 저장된 것부터 삭제 |
| LRU | 마지막 사용 시점 | 오래 사용하지 않은 것부터 삭제 |
| LFU | 사용 횟수 | 적게 사용된 것부터 삭제 |
| Random | 무작위 | 아무거나 골라 삭제 |

LRU는 “최근에 사용한 데이터는 곧 다시 사용할 가능성이 높다”는 가정에 기반합니다.

```text
최근 사용: A, B, C
오래 안 씀: D

공간이 부족하면 D 삭제
```

LFU는 “자주 사용된 데이터는 앞으로도 중요할 가능성이 높다”는 가정에 기반합니다.

```text
A: 100번 사용
B: 3번 사용
C: 1번 사용

공간이 부족하면 C 삭제
```

LRU와 LFU는 서로 장단점이 있습니다. LRU는 최근 흐름에 민감하고 구현이 비교적 단순합니다. LFU는 장기적으로 자주 쓰인 데이터를 지키는 데 유리하지만, 사용 횟수 관리가 더 복잡하고 오래된 인기 데이터가 계속 남는 문제가 생길 수 있습니다.

## 19. TTL과 만료 전략

TTL은 Time To Live의 약자입니다. 데이터가 살아 있을 시간을 뜻합니다.

```text
token = abc
TTL = 60초

60초가 지나면 token은 만료됨
```

TTL이 필요한 이유는 많습니다.

| 상황 | TTL이 필요한 이유 |
| --- | --- |
| 로그인 세션 | 일정 시간이 지나면 다시 로그인하게 함 |
| 인증 토큰 | 오래된 토큰을 자동으로 무효화 |
| 임시 캐시 | 오래된 데이터를 계속 보여주지 않음 |
| 요청 제한 | 일정 시간 동안의 카운트를 관리 |

만료된 데이터를 지우는 방식은 크게 두 가지로 나눌 수 있습니다.

| 방식 | 설명 | 장점 | 단점 |
| --- | --- | --- | --- |
| Lazy expiration | 조회나 명령 실행 시 만료 여부 확인 | 구현이 단순하고 평소 비용이 적음 | 만료된 데이터가 잠시 남아 있을 수 있음 |
| Active expiration | 주기적으로 만료된 데이터를 찾아 삭제 | 메모리를 더 빨리 회수 | 백그라운드 작업 비용이 듦 |

최소 힙은 TTL 관리에 잘 맞습니다. “가장 빨리 만료될 데이터”가 항상 위에 있기 때문입니다.

```text
만료 시간:
A: 30초 뒤
B: 5초 뒤
C: 20초 뒤

최소 힙의 맨 위: B
```

힙을 쓰지 않고 모든 key를 매번 확인하면 key가 많을수록 느려집니다. 힙을 쓰면 가장 가까운 만료 후보부터 확인할 수 있습니다.

## 20. 힙, 정렬 배열, 트리의 차이

가장 작은 값을 찾고 싶을 때 사용할 수 있는 구조는 여러 가지입니다.

| 구조 | 가장 작은 값 찾기 | 삽입 | 삭제 | 특징 |
| --- | --- | --- | --- | --- |
| 정렬되지 않은 배열 | O(N) | O(1) | O(N) | 넣기는 쉽지만 찾기가 느림 |
| 정렬된 배열 | O(1) | O(N) | O(N) | 찾기는 쉽지만 중간 삽입이 느림 |
| 최소 힙 | O(1) | O(log N) | O(log N) | 최솟값을 계속 꺼내는 데 좋음 |
| 균형 잡힌 BST | O(log N) | O(log N) | O(log N) | 정렬 순회와 범위 검색에 좋음 |

힙은 전체 정렬을 보장하지 않습니다. 오직 부모가 자식보다 작다는 규칙만 보장합니다.

```text
        1
       / \
      4   2
     / \
    7   5
```

위 구조는 최소 힙입니다. 하지만 왼쪽에서 오른쪽으로 읽는다고 정렬된 순서가 나오지는 않습니다.

```text
배열 표현: [1, 4, 2, 7, 5]
정렬 결과: [1, 2, 4, 5, 7]
```

즉, 힙은 “전체를 정렬해서 보고 싶다”보다 “지금 가장 작은 것만 빠르게 알고 싶다”에 더 적합합니다.

## 21. 메모리 사용량과 오버헤드

데이터를 저장할 때 메모리를 차지하는 것은 실제 값만이 아닙니다.

```text
key = "name"
value = "Alice"
```

겉으로 보기에는 `"name"` 4글자와 `"Alice"` 5글자만 저장하는 것처럼 보입니다. 하지만 자료구조 안에서는 더 많은 정보가 필요할 수 있습니다.

```text
key bytes
value bytes
다음 노드를 가리키는 정보
이전 노드를 가리키는 정보
해시맵 엔트리 객체
버킷 배열의 빈 칸
TTL 만료 시간
사용 횟수나 최근 사용 정보
```

이런 부가 비용을 오버헤드라고 부릅니다.

쉬운 비유로, 택배 물건 자체만 무게가 있는 것이 아니라 상자, 완충재, 송장도 무게가 있는 것과 비슷합니다. 실제 시스템에서는 데이터 자체보다 오버헤드가 더 크게 느껴지는 경우도 있습니다.

메모리 계산 방식은 목적에 따라 달라집니다.

| 계산 방식 | 설명 |
| --- | --- |
| 단순 모델 | key와 value 크기만 계산 |
| 자료구조 포함 모델 | 노드, 포인터, 버킷, 힙 레코드까지 계산 |
| 실제 런타임 모델 | 언어 런타임의 객체 크기와 할당 단위까지 고려 |

학습용 과제에서는 단순 모델이 이해하기 쉽습니다. 실무에서는 자료구조 오버헤드와 런타임 오버헤드까지 고려해야 메모리 사용량을 더 현실적으로 볼 수 있습니다.

## 22. Mini Redis와 연결해서 보기

이제 각 자료구조가 과제에서 어떤 의미로 연결되는지만 가볍게 정리하면 됩니다.

| 자료구조 | 쉬운 의미 | 과제에서의 연결 |
| --- | --- | --- |
| 동적 배열 | 필요하면 커지는 배열 | 해시맵과 힙의 내부 저장 공간 |
| 해시맵 | 이름표로 빠르게 찾는 사전 | key로 value 찾기 |
| 이중 연결 리스트 | 앞뒤로 연결된 줄 | 최근 사용 순서 관리 |
| LRU | 오래 안 쓴 것부터 지우는 규칙 | 메모리 초과 시 삭제 기준 |
| 큐 | 먼저 온 것부터 처리 | Pub/Sub 메시지 버퍼 이해 |
| 스택 | 마지막에 온 것부터 처리 | 자료구조 비교 학습 |
| 덱 | 양쪽 끝을 모두 쓰는 줄 | LRU를 이해하는 데 도움 |
| 최소 힙 | 가장 작은 값을 맨 위에 둠 | 가장 빨리 만료될 key 찾기 |
| 트리 | 부모-자식 계층 구조 | 순회 학습 |
| BST | 찾기 쉽게 정렬 규칙이 있는 트리 | 탐색/정렬 구조 학습 |

## 23. 핵심 정리

이 과제에서 가장 중요한 연결은 세 가지입니다.

```text
1. 빠르게 찾기
   -> 해시맵

2. 오래 안 쓴 데이터 찾기
   -> 이중 연결 리스트와 LRU

3. 가장 빨리 만료될 데이터 찾기
   -> 최소 힙
```

Redis를 이해할 때도 같은 관점이 중요합니다. Redis는 단순히 데이터를 저장하는 프로그램이 아니라, 상황에 맞는 자료구조를 사용해 데이터를 빠르게 찾고, 지우고, 만료시키는 시스템입니다.
